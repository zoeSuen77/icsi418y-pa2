# Replace this with your MongoDB Atlas connection string for the course setup.
# Keep your real credentials in server/.env, which is ignored by Git.
MONGO_URI=mongodb://127.0.0.1:27017
DB_NAME=pa2
PORT=9000
CLIENT_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
